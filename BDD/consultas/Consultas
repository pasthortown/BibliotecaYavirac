1) CUANTOS EJEMPLARES POR CADA TIPO DE RECURSO TENGO?
SELECT TipoRecurso.descripcion, COUNT(Recurso.id) AS cantidad FROM Recurso INNER JOIN TipoRecurso ON (Recurso.idTipo=TipoRecurso.id) GROUP BY idTipo;

2) CUANTOS LIBROS HAN SIDO SOLICITADOS Y AUN NO REGRESAN?
SELECT TipoRecurso.descripcion,COUNT(Recurso.id) FROM Recurso INNER JOIN TipoRecurso ON (Recurso.idTipo=TipoRecurso.id) INNER JOIN (DetalleSolicitud) ON (DetalleSolicitud.idRecurso=Recurso.id) INNER JOIN (Solicitud) ON (DetalleSolicitud.idSolicitud=Solicitud.id) WHERE Solicitud.fechaDevolucion='NULL' GROUP BY idTipo;

3) CUANTOS LIBROS HAN SIDO SOLICITADOS EN EL ULTIMO MES, AÑO, HORA Y DIA DE LA SEMANA?
SELECT idTipo,COUNT(Recurso.id) AS cantidad FROM Recurso INNER JOIN (DetalleSolicitud) ON (DetalleSolicitud.idRecurso=Recurso.id) WHERE idTipo=1 BETWEEN '2018-03-23 00:00:00' AND '2017-03-23 23:59:59';

4) CUANTOS LIBROS TENGO POR CADA ESTADO?
SELECT Estado.descripcion,COUNT(Recurso.id) FROM Recurso INNER JOIN TipoRecurso ON (Recurso.idTipo=TipoRecurso.id) INNER JOIN Ejemplar ON (Recurso.id=Ejemplar.id) JOIN Estado ON (Ejemplar.id=Estado.id) GROUP BY estado.id;


5)CUANTOS LIBROS TENGO POR CADA CATEGORIA DE LA UNESCO?
SELECT CategoriaRecurso.descripcion AS 'Categoria Unesco', COUNT(Recurso.id) AS Cantidad FROM Recurso INNER JOIN CategoriaRecurso ON (CategoriaRecurso.id=Recurso.idCategoria) GROUP BY CategoriaRecurso; 